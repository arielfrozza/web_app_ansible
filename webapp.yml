- name: Deploy a web apllication
  hosts: ubuntu,centos
  tasks:
  - name: Install all requided dependencies
    apt: name={{ item }} state=installed
    with_items:
      - python
      - python-setuptools
      - python-dev
      - build-essencial
      - python-pip 
